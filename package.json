{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "chmod +x node_modules/.bin/nuxt && nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "^1.7.0",
    "@types/node": "^18.17.1",
    "nuxt": "^3.6.5"
  },
  "dependencies": {
    "swiper": "^8.4.6"
  }
}
